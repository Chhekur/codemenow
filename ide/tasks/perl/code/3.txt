function multiplyNumbers(){

        //Variables in Javascript are without a type, \nhence an Int (Number) or 
        //String (Text) can be defined with the same method as shown below

        var x = 2;
        x = x * 11;

        //Create a variable named y, set its value to 8

	

        //Multiply both variables, x and y and then return it
        // eg. 'return a * b' returns the product of a and b
        return 
}
multiplyNumbers();